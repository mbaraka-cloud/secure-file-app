[pytest]
addopts = -q
testpaths = tests
markers =
    slow: tests that are slow
    integration: integration tests (need app routes)
    stripe: tests hitting stripe webhook (mocked)
    security: security-related tests
    encryption: encryption-related tests
    billing: billing/credits-related tests
    sharing: file sharing/download-related tests
